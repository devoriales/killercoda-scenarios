# Pinned Python toolchain — every developer shares the exact same versions.
# Bump deliberately in a reviewed PR; do not chase latest. Reproducibility beats novelty.
ansible-core==2.20.5
ansible-lint==24.12.2
pre-commit==4.0.1
detect-secrets==1.5.0
