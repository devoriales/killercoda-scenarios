---
# Pinned Ansible collections the playbooks rely on. Installed with:
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix
    version: "2.0.0"
  - name: community.general
    version: "10.1.0"
  - name: community.docker
    version: "4.4.0"
